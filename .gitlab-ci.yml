image: google/cloud-sdk:alpine

deploy-prod:
  stage: deploy
  script:
    - gcloud auth activate-service-account --key-file="$GCP_SERVICE_ACCOUNT_KEY"
    - gcloud config set project pt-report-451612
    - gcloud --quiet app deploy app.yaml
  environment: production
  only:
    - main